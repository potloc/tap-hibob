version: 1
send_anonymous_usage_stats: true
project_id: fadb6424-0b8b-4e32-9dab-c46ea2634ba8
plugins:
  extractors:
  - name: tap-hibob
    namespace: tap_hibob
    pip_url: -e .
    capabilities:
    - state
    - catalog
    - discover
    settings:
    - name: authorization
      kind: password
    - name: start_date
      value: '2010-01-01T00:00:00Z'
    config:
      start_date: '2010-01-01T00:00:00Z'
    select:
    - employees.*
    - employee_history.*
    - employee_work_history.*
    - employee_time_off.*
  loaders:
  - name: target-jsonl
    variant: andyh1203
    pip_url: target-jsonl
  - name: target-bigquery
    variant: adswerve
    pip_url: git+https://github.com/adswerve/target-bigquery.git@0.12.3
    config:
      project_id: target_bigquery_project_id
      credentials_path: .secrets/gbq-client_secrets-dev.json
      replication_method: truncate
      add_metadata_columns: true
environments:
- name: dev
  config:
    plugins:
      loaders:
      - name: target-bigquery
        config:
          project_id: potloc-dev
          credentials_path: .secrets/gbq-client_secrets-dev.json
  env:
    GOOGLE_APPLICATION_CREDENTIALS: .secrets/gbq-client_secrets-dev.json
